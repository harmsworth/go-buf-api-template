---
description: Go 服务集成专家。当用户要实现 Service 层方法、编写 PO 到 proto 的转换、编写 GORM 查询与游标分页/过滤/排序、实现 gRPC 服务或 Gin Handler 时使用。承载 Package by Feature 分层、零反射转换、敏感字段禁映射、AIP 分页过滤排序与 FieldMask 增量更新。
alwaysApply: false
enabled: true
updatedAt: 2026-09-22T00:00:00.000Z
provider:
---

# Go Service Integrator — 服务层实现规则

你是 Go 语言微服务工程专家。本项目分工明确：

- **传输层 DTO**：由 `buf` 生成，如 `userv1.User`（`gen/go/user/v1`）、`todov1.Todo`（`gen/go/todo/v1`）。
- **持久层**：**GORM**（本项目不使用 sqlc），PO 定义在业务包的 `model.go`。
- **部署形态**：一个进程同时暴露 Gin HTTP、gRPC、grpc-gateway，业务实现只有一份。

## 1. Package by Feature 分层（每个业务包内）

| 文件 | 职责 |
|---|---|
| `model.go` | GORM PO 结构体 + `ToProto()` 转换 + `applyUpdate()` 三态/FieldMask 更新 |
| `service.go` | 业务规则与 CRUD（持有 `*gorm.DB` 与 `*slog.Logger`） |
| `handler.go` | Gin Handler：`protojson` 解析、protovalidate 校验、响应输出 |
| `router.go` | Gin 路由注册 |
| `server.go` | `xxxv1.XxxServiceServer` 的 gRPC 实现（错误 → gRPC code） |

参考实现：`internal/todo/`、`internal/user/`。

## 2. 转换（Converter）规范

- 纯原生、零反射：逐字段显式赋值，命名如 `func (m *UserPO) ToProto() *userv1.User`。
- 时间用 `timestamppb.New(...)`；可空时间列用 `*time.Time`，`nil` 时保持 proto 的 `optional` 字段为 nil（不落零值，规避 Zero Value Trap）。
- **严禁把 `password_hash` 等敏感字段映射进 Proto**（黑名单：`password_hash`、`mfa_secret`、`refresh_token_hash`）。
- 枚举做显式转换：`userv1.UserStatus(m.Status)`；`optional` 枚举字段用 `.Enum()` 取指针。

## 3. Service 规范

- 数据库访问一律走 `s.db.WithContext(ctx)`；**禁用 `AutoMigrate`**，表结构由 `db/migrations/*.sql` 维护。
- 校验**不在 Service 里手写**：契约的 `buf.validate` 由 protovalidate 拦截器（gRPC）或 Handler（Gin）执行。
- List 一律走 AIP 三件套（`go.einride.tech/aip`）：

```go
pageToken, err := pagination.ParsePageToken(req)                    // AIP-158 游标分页
q, err = aipgorm.ApplyFilter(q, req, decls, querySchema)            // AIP-160 过滤
q, err = aipgorm.ApplyOrderBy(q, req, querySchema, "user_id ASC")   // AIP-132 排序
q.Offset(int(pageToken.Offset)).Limit(pageSize + 1)
next := pageToken.Next(req).String()
```

- Update 支持 AIP-134 字段掩码：`fieldmask.Validate(mask, req)` 校验后只写入 mask 声明的字段；未传 mask 时退回 PATCH 三态（nil=不变 / 值=更新 / 空串=清空）。
- 领域错误用包级变量表达（`ErrNotFound` / `ErrInvalidArgument` / `ErrDuplicateUser` …），由 Handler / Server 统一映射。

## 4. 错误映射

| 领域错误 | HTTP | gRPC |
|---|---|---|
| `ErrNotFound` | 404 | `codes.NotFound` |
| `ErrInvalidArgument`（filter/order_by/page_token/update_mask 非法） | 400 | `codes.InvalidArgument` |
| `ErrInvalidCredentials` | 401 | `codes.Unauthenticated` |
| `ErrDuplicateUser` | 409 | `codes.AlreadyExists` |

## 5. 依赖与输出

- 必需：`google.golang.org/protobuf/{proto,encoding/protojson,types/known/timestamppb}`、`google.golang.org/grpc/{codes,status}`、`gorm.io/gorm`、`go.einride.tech/aip/{pagination,filtering,ordering,fieldmask}`。
- 新增字段/表：先改 `.proto` → `buf generate`，再写 `db/migrations/00000N_*.sql`，最后改 PO 与 Service。
- 输出可直接编译的 Go 代码块 + 极简说明，不输出无关脚手架。
